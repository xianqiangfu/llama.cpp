# 安全策略

 - [**报告漏洞**](#报告漏洞)
 - [**要求**](#要求)
 - [**涵盖的主题**](#涵盖的主题)
 - [**安全地使用 llama.cpp**](#安全地使用-llamacpp)
   - [不受信任的模型](#不受信任的模型)
   - [不受信任的输入](#不受信任的输入)
   - [数据隐私](#数据隐私)
   - [不受信任的环境或网络](#不受信任的环境或网络)
   - [多租户环境](#多租户环境)

## 报告漏洞

如果您在此项目中发现了属于[涵盖的主题](#涵盖的主题)范围内的安全漏洞，请私下报告。**不要将其公开为问题。** 这使我们有时间在公开披露之前与您一起解决问题，从而减少利用在补丁发布前被使用的可能性。

请将其作为私有[安全建议](https://github.com/ggml-org/llama.cpp/security/advisories/new)披露。

该项目由一组志愿者在尽力而为的基础上维护。因此，在公开披露之前，请至少给我们 90 天的时间来处理修复。

> [!IMPORTANT]
> 对于协作者：如果您有兴趣帮助审查私有安全披露，请参阅：https://github.com/ggml-org/llama.cpp/discussions/18080

## 要求

在提交报告之前，请确保您满足以下要求：

- 您已阅读此政策并充分理解它。
- AI 仅允许按照 [AGENTS.md](AGENTS.md) 中的说明以辅助角色使用。我们不接受完全由 AI 编写的报告。
- 您的报告必须包含有效的概念验证，形式为脚本和/或附加文件。

如果未满足这些要求，维护者有权关闭报告。

## 涵盖的主题

只有属于项目这些部分内的漏洞才被视为有效。对于超出此列表的问题，请将它们作为问题报告。

- `src/**/*`
- `ggml/**/*`
- `gguf-py/**/*`
- `tools/server/*`，**不包括**以下主题：
    - Web UI
    - 标记为实验性的功能
    - 不建议在不受信任的环境中使用的功能（例如，路由器、MCP）
    - 可能导致拒绝服务攻击的错误

请注意，[安全地使用 llama.cpp](#安全地使用-llamacpp) 下的任何主题都不被视为 LLaMA C++ 中的漏洞。

对于 `vendor` 目录中的漏洞，请直接报告给第三方项目。

## 安全地使用 llama.cpp

### 不受信任的模型
运行不受信任的模型时要小心。此分类包括由未知开发人员创建或使用从未知来源获得的数据的模型。

*始终在安全的隔离环境中执行不受信任的模型，例如沙箱*（例如，容器、虚拟机）。这有助于保护您的系统免受潜在恶意代码的侵害。

> [!NOTE]
> 模型的可信度不是二元的。您必须根据特定模型以及它如何匹配您的用例和风险承受能力，始终确定适当的谨慎级别。

### 不受信任的输入

某些模型接受各种输入格式（文本、图像、音频等）。转换这些输入的库具有不同的安全级别，因此隔离模型并仔细预处理输入以减轻脚本注入风险至关重要。

在处理不受信任的输入时，为了最大程度地提高安全性，您可能需要采用以下方法：

* 沙箱：隔离发生推理的环境。
* 预分析：检查模型在暴露于提示词注入时的默认行为（例如，使用[模糊测试进行提示词注入](https://github.com/FonduAI/awesome-prompt-injection?tab=readme-ov-file#tools)）。这将为您需要在下一个主题上做多少工作提供线索。
* 更新：保持 LLaMA C++ 和您的库使用最新的安全补丁更新。
* 输入清理：在将数据提供给模型之前，严格清理输入。这涉及以下技术：
    * 验证：对允许的字符和数据类型执行严格规则。
    * 过滤：删除潜在的恶意脚本或代码片段。
    * 编码：将特殊字符转换为安全表示。
    * 验证：运行识别潜在脚本注入的工具（例如，[检测提示词注入尝试的模型](https://python.langchain.com/docs/guides/safety/hugging_face_prompt_injection)）。

### 数据隐私

为了保护敏感数据免受潜在泄露或未经授权的访问，至关重要的是对模型执行进行沙箱处理。这意味着在安全的隔离环境中运行模型，这有助于减轻许多攻击向量。

### 不受信任的环境或网络

如果您无法在安全的隔离环境中运行模型，或者必须将其暴露给不受信任的网络，请务必采取以下安全预防措施：
* 不要使用 RPC 后端，[rpc-server](https://github.com/ggml-org/llama.cpp/tree/master/tools/rpc) 和 [llama-server](https://github.com/ggml-org/llama.cpp/tree/master/tools/server) 功能（参见 https://github.com/ggml-org/llama.cpp/pull/13061）。
* 确认任何下载的制品（例如，预训练模型权重）的哈希值与已知良好的值匹配。
* 如果通过网络发送数据，请加密数据。

### 多租户环境

如果您打算在共享内存中并行运行多个模型，您有责任确保模型之间不交互或访问彼此的数据。主要关注领域是租户隔离、资源分配、模型共享和硬件攻击。

1. 租户隔离：模型应使用强隔离方法单独运行，以防止不必要的数据访问。分离网络对于隔离至关重要，因为它防止未经授权访问数据或模型，以及恶意用户以另一个租户的身份发送图表进行执行。

2. 资源分配：一个模型导致的拒绝服务可能会影响整个系统的健康状况。实施保障措施，如速率限制、访问控制和健康监控。

3. 模型共享：在多租户模型共享设计中，租户和用户必须了解运行他人提供的代码的安全风险。由于没有可靠的方法来检测恶意模型，因此对模型执行进行沙箱处理是缓解风险的推荐方法。

4. 硬件攻击：GPU 或 TPU 也可以被攻击。[研究](https://scholar.google.com/scholar?q=gpu+side+channel)表明，GPU 上的侧信道攻击是可能的，这可能导致同时在同一系统上运行的其他模型或进程的数据泄露。
