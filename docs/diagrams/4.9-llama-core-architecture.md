# llama.cpp 总体模块架构图

## 架构图

```mermaid
graph TB
    subgraph 应用层[应用层]
        APP1[主程序：main]
        APP2[补全工具：llama-cli]
        APP3[服务器：llama-server]
        APP4[测试工具：llama-bench]
        APP5[其他示例程序]
    end

    subgraph 工具层[工具层]
        T1[量化工具]
        T2[GGUF转换工具]
        T3[脚本工具]
        T4[编译工具]
        T5[部署工具]
    end

    subgraph 核心库层[核心库层]
        L1[llama库<br/>核心推理引擎]
        L2[ggml库<br/>张量计算框架]
        L3[gguf库<br/>文件格式处理]
        L4[common库<br/>公共工具函数]
    end

    subgraph 后端层[后端层]
        B1[CUDA后端]
        B2[Metal后端]
        B3[Vulkan后端]
        B4[HIP后端]
        B5[BLAS后端]
        B6[其他后端<br/>OpenCL/SYCL等]
    end

    subgraph 硬件层[硬件层]
        H1[NVIDIA GPU]
        H2[Apple Silicon]
        H3[AMD GPU]
        H4[Intel GPU]
        H5[CPU]
    end

    APP1 --> L1
    APP2 --> L1
    APP3 --> L1
    APP4 --> L1
    APP5 --> L1

    L1 --> L2
    L1 --> L3
    L1 --> L4

    L2 --> B1
    L2 --> B2
    L2 --> B3
    L2 --> B4
    L2 --> B5
    L2 --> B6

    B1 --> H1
    B2 --> H2
    B3 --> H3
    B4 --> H4
    B5 --> H5
    B6 --> H5

    T1 --> L1
    T2 --> L3
    T3 --> L4
    T4 --> L4
    T5 --> L4

    style APP1 fill:#e1f5ff
    style APP2 fill:#e1f5ff
    style APP3 fill:#e1f5ff
    style APP4 fill:#e1f5ff
    style APP5 fill:#e1f5ff
    style L1 fill:#fff4e1
    style L2 fill:#fff4e1
    style L3 fill:#fff4e1
    style L4 fill:#fff4e1
```

## 模块详细说明

### 1. 应用层
- **main.c**: 主程序，提供命令行界面
- **llama-cli**: 补全生成工具，支持交互式和批处理
- **llama-server**: HTTP API服务器，支持RESTful接口
- **llama-bench**: 性能基准测试工具
- **示例程序**: 各种功能演示和测试程序

### 2. 工具层
- **量化工具**: 模型量化，支持多种量化格式
- **GGUF转换工具**: 格式转换和模型处理
- **脚本工具**: 辅助开发和部署的脚本
- **编译工具**: 构建和依赖管理

### 3. 核心库层

#### llama库（核心推理引擎）
- 模型加载和初始化
- 推理循环和采样
- KV Cache管理
- 多模态支持
- 语法约束

#### ggml库（张量计算框架）
- 张量数据结构
- 数学运算实现
- 内存管理
- 量化操作
- 优化算法

#### gguf库（文件格式处理）
- GGUF格式解析
- 元数据管理
- 模型序列化/反序列化
- 版本兼容性处理

#### common库（公共工具函数）
- 字符串处理
- 时间和日志
- 配置管理
- 工具函数

### 4. 后端层
- **CUDA**: NVIDIA GPU加速
- **Metal**: Apple Silicon GPU加速
- **Vulkan**: 跨平台GPU加速
- **HIP**: AMD GPU加速
- **BLAS**: CPU BLAS库加速
- **其他**: OpenCL、SYCL等

### 5. 硬件层
- **NVIDIA GPU**: CUDA后端
- **Apple Silicon**: Metal后端
- **AMD GPU**: HIP/Vulkan后端
- **Intel GPU**: Vulkan/SYCL后端
- **CPU**: BLAS和其他CPU后端

## 设计特点

1. **模块化设计**: 清晰的层次结构，便于维护和扩展
2. **硬件抽象**: 通过后端层实现硬件无关性
3. **可扩展性**: 支持添加新的后端和功能
4. **性能优化**: 针对不同硬件进行专门的优化
5. **跨平台**: 支持多种操作系统和硬件平台

## 依赖关系

```
应用层
  ↓ 依赖
核心库层
  ↓ 调用
后端层
  ↓ 访问
硬件层
```

工具层可以独立于应用层使用，也可以与核心库层集成。