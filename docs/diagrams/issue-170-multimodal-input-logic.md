# 多模态输入处理逻辑图

```mermaid
flowchart TD
    A[接收多模态输入] --> B{识别输入类型}
    B -->|文本| C[文本分词]
    B -->|图像| D[图像编码]
    B -->|音频| E[音频编码]
    B -->|多模态组合| F[组合输入处理]

    C --> G[获取文本 token IDs]
    D --> H[获取图像嵌入]
    E --> I[获取音频嵌入]
    F --> J[分离各类输入]

    G --> K[构建文本位置编码]
    H --> L[构建图像位置编码]
    I --> M[构建音频位置编码]
    J --> N[分别处理各类输入]

    K --> O[合并到批次]
    L --> P[应用 M-RoPE]
    M --> Q[应用专用位置编码]
    N --> P

    P --> R[构建统一批次]
    Q --> R
    O --> R

    R --> S{检查位置编码维度}
    S -->|1D| T[使用标准位置编码]
    S -->|2D| U[使用 M-RoPE 编码]
    S -->|3D+| V[使用 IM-RoPE 编码]

    T --> W[创建 llama_batch]
    U --> W
    V --> W

    W --> X[设置 n_pos 参数]
    X --> Y[填充位置数据]
    Y --> Z[填充序列ID]
    Z --> AA[标记输出位置]
    AA --> AB[准备前向传播]
    AB --> AC[结束]

    subgraph M-RoPE 编码
        AD[位置0] --> AE[序列位置]
        AD --> AF[Y坐标位置]
        AD --> AG[X坐标位置]
        AD --> AH[其他维度]
    end

    U -.-> AD
```

## 核心组件

### 1. 输入类型处理
- **文本分词**: 使用词汇表将文本转换为 token IDs
- **图像编码**: 视觉编码器将图像转换为嵌入向量
- **音频编码**: 音频编码器将音频转换为嵌入向量

### 2. 位置编码机制
- **1D 编码**: 标准序列位置编码
- **M-RoPE**: 多维度 RoPE 位置编码（图像等 2D 输入）
- **IM-RoPE**: 交错多维度位置编码（复杂多模态场景）

### 3. 批次构建
- **n_pos 参数**: 每个token的位置编码数量
- **位置数据**: 包含多个维度的位置信息
- **序列ID**: 标识每个token所属的序列

### 4. 关键特性
- 支持混合模态输入
- 灵活的位置编码配置
- 统一的批次处理接口