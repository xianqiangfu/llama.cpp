# 量化模块架构图

## 量化架构图

```mermaid
graph TB
    subgraph 量化模块[量化模块]
        subgraph 核心功能[核心功能]
            QF1[量化算法]
            QF2[反量化]
            QF3[量化感知训练]
            QF4[混合精度]
        end

        subgraph 量化格式[量化格式]
            QF1[Q4_0]
            QF2[Q4_1]
            QF3[Q5_0]
            QF4[Q5_1]
            QF5[Q8_0]
            QF6[Q2_K]
            QF7[Q3_K]
            QF8[Q4_K]
            QF9[Q5_K]
            QF10[Q6_K]
            QF11[Q8_K]
        end

        subgraph 量化流程[量化流程]
            QP1[权重收集]
            QP2[统计计算]
            QP3[量化参数计算]
            QP4[权重量化]
            QP5[校准优化]
        end

        subgraph 反量化流程[反量化流程]
            DQ1[读取量化参数]
            DQ2[加载量化权重]
            DQ3[反量化计算]
            DQ4[精度恢复]
        end
    end

    QF1 --> QP1
    QP1 --> QP2
    QP2 --> QP3
    QP3 --> QP4
    QP4 --> QP5

    QP5 --> DQ1
    DQ1 --> DQ2
    DQ2 --> DQ3
    DQ3 --> DQ4

    QF1 --> QF1
    QF2 --> QF2
    QF3 --> QF3
    QF4 --> QF4
    QF5 --> QF5
    QF6 --> QF6
    QF7 --> QF7
    QF8 --> QF8
    QF9 --> QF9
    QF10 --> QF10
    QF11 --> QF11

    style QP1 fill:#e1f5ff
    style DQ4 fill:#c8e6c9
```

## 量化算法流程

```mermaid
graph TB
    subgraph 量化算法[量化算法]
        INPUT[原始权重] --> ANALYZE{权重分析}
        ANALYZE --> DISTRIBUTION[分布分析]
        ANALYZE --> RANGE[数值范围]
        ANALYZE --> OUTLIERS[异常值检测]

        DISTRIBUTION --> SELECT{选择量化方法}
        RANGE --> SELECT
        OUTLIERS --> SELECT

        SELECT --> S1[对称量化]
        SELECT --> S2[非对称量化]
        SELECT --> S3[块量化]
        SELECT --> S4[分组量化]

        S1 --> QUANT[量化计算]
        S2 --> QUANT
        S3 --> QUANT
        S4 --> QUANT

        QUANT --> OPTIMIZE{优化策略}
        OPTIMIZE --> O1[参数优化]
        OPTIMIZE --> O2[范围调整]
        OPTIMIZE --> O3[块大小优化]

        O1 --> OUTPUT[量化权重]
        O2 --> OUTPUT
        O3 --> OUTPUT
    end

    style OUTPUT fill:#c8e6c9
```

## 量化格式对比

| 格式 | 比特数 | 压缩比 | 精度 | 适用场景 |
|------|--------|--------|------|----------|
| Q4_0 | 4 | 4x | 中等 | 通用场景 |
| Q4_1 | 4 | 4x | 较高 | 高精度需求 |
| Q5_0 | 5 | 3.2x | 较高 | 平衡选择 |
| Q5_1 | 5 | 3.2x | 高 | 精度优先 |
| Q8_0 | 8 | 2x | 高 | 高精度场景 |
| Q2_K | 2 | 8x | 低 | 极致压缩 |
| Q3_K | 3 | 5.3x | 较低 | 平衡压缩 |
| Q4_K | 4 | 4x | 中等 | 通用场景 |
| Q5_K | 5 | 3.2x | 较高 | 高性能场景 |
| Q6_K | 6 | 2.7x | 高 | 精度优先 |
| Q8_K | 8 | 2x | 很高 | 最高精度 |

## 量化参数计算

```mermaid
graph TB
    subgraph 参数计算[量化参数计算]
        STATS[统计信息] --> SCALE{缩放因子}
        STATS --> ZERO{零点偏移}
        STATS --> MIN{最小值}
        STATS --> MAX{最大值}

        SCALE --> S1[S = (max - min) / (2^n - 1)]
        ZERO --> Z1[Z = round(-min / S)]
        MIN --> M1[保持原始范围]
        MAX --> M2[确保覆盖范围]

        S1 --> APPLY[应用参数]
        Z1 --> APPLY
        M1 --> APPLY
        M2 --> APPLY

        APPLY --> VERIFY{验证精度}
        VERIFY --> |满意| FINAL[最终参数]
        VERIFY --> |不满意| ADJUST[调整策略]

        ADJUST --> SCALE

        style FINAL fill:#c8e6c9
    end
```

## 混合精度量化

```mermaid
graph TB
    subgraph 混合精度[混合精度量化]
        MODEL[模型分析] --> LAYER{层级分析}
        LAYER --> |敏感层| L1[高精度量化]
        LAYER --> |一般层| L2[中等精度量化]
        LAYER --> |不敏感层| L3[低精度量化]

        L1 --> FORMAT1{格式选择}
        L2 --> FORMAT2{格式选择}
        L3 --> FORMAT3{格式选择}

        FORMAT1 --> F1[Q8_K/Q6_K]
        FORMAT2 --> F2[Q5_K/Q4_K]
        FORMAT3 --> F3[Q3_K/Q2_K]

        F1 --> OPTIMIZE[性能优化]
        F2 --> OPTIMIZE
        F3 --> OPTIMIZE

        OPTIMIZE --> BALANCE{平衡策略}
        BALANCE --> B1[精度优先]
        BALANCE --> B2[速度优先]
        BALANCE --> B3[平衡模式]

        B1 --> FINAL[混合量化方案]
        B2 --> FINAL
        B3 --> FINAL

        style FINAL fill:#c8e6c9
    end
```

## 量化感知训练

```mermaid
graph TB
    subgraph 量化感知训练[量化感知训练]
        TRAIN[训练过程] --> FAKE[伪量化层]
        FAKE --> SIMULATE[模拟量化误差]
        SIMULATE --> GRADIENT[梯度累积]
        GRADIENT --> UPDATE[权重更新]

        UPDATE --> EVAL{评估精度}
        EVAL --> |精度满意| DEPLOY[部署模型]
        EVAL --> |精度不足| RETRAIN[重新训练]

        RETRAIN --> TUNE{调整策略}
        TUNE --> T1[调整学习率]
        TUNE --> T2[调整量化粒度]
        TUNE --> T3[调整量化方法]

        T1 --> TRAIN
        T2 --> TRAIN
        T3 --> TRAIN

        style DEPLOY fill:#c8e6c9
    end
```

## 量化工具流程

```mermaid
graph TB
    subgraph 量化工具[量化工具流程]
        INPUT[输入模型] --> LOAD[加载模型]
        LOAD --> ANALYZE[模型分析]
        ANALYZE --> CONFIG[配置量化参数]

        CONFIG --> FORMAT{量化格式}
        FORMAT --> F1[选择格式]
        FORMAT --> F2[设置参数]

        F1 --> QUANTIZE[量化执行]
        F2 --> QUANTIZE

        QUANTIZE --> CALIBRATE{校准？}
        CALIBRATE --> |是| CAL[校准数据]
        CALIBRATE --> |否| SAVE[保存模型]

        CAL --> TEST{测试精度}
        TEST --> |满意| SAVE
        TEST --> |不满意| RETRY[重新配置]

        RETRY --> FORMAT

        SAVE --> VALIDATE[验证模型]
        VALIDATE --> DEPLOY[部署使用]

        style DEPLOY fill:#c8e6c9
    end
```

## 量化性能优化

```mermaid
graph TB
    subgraph 性能优化[量化性能优化]
        OPTIMIZE[优化策略] --> MEM[内存优化]
        OPTIMIZE --> COMPUTE[计算优化]
        OPTIMIZE --> BANDWIDTH[带宽优化]

        MEM --> M1[减少内存占用]
        MEM --> M2[提高缓存命中率]
        MEM --> M3[优化内存布局]

        COMPUTE --> C1[SIMD优化]
        COMPUTE --> C2[GPU加速]
        COMPUTE --> C3[专用指令]

        BANDWIDTH --> B1[减少数据传输]
        BANDWIDTH --> B2[压缩传输数据]
        BANDWIDTH --> B3[异步传输]

        M1 --> RESULT[性能提升]
        M2 --> RESULT
        M3 --> RESULT
        C1 --> RESULT
        C2 --> RESULT
        C3 --> RESULT
        B1 --> RESULT
        B2 --> RESULT
        B3 --> RESULT

        style RESULT fill:#c8e6c9
    end
```

## 配置参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `quant_type` | 量化格式 | Q4_K |
| `imatrix_file` | 重要性矩阵文件 | - |
| `f16_kv` | KV Cache是否使用F16 | true |
| `group_size` | 量化分组大小 | 128 |
| `quant_method` | 量化方法 | k-quants |
| `calibrate_samples` | 校准样本数 | 100 |

## 扩展性

### 添加新量化格式
1. 定义新的量化结构
2. 实现量化和反量化函数
3. 集成到量化框架
4. 测试和优化

### 自定义量化策略
- 实现自定义量化算法
- 添加量化感知训练
- 支持混合精度策略
- 优化特定场景量化

### 性能调优
- 优化计算内核
- 改进内存访问模式
- 利用硬件特性
- 提高量化精度