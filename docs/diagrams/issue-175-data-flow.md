# 数据流向图

```mermaid
flowchart TD
    A[用户输入<br/>文本/图像/音频] --> B[输入预处理]
    B --> C{输入类型}
    C -->|文本| D[文本分词]
    C -->|图像| E[图像编码]
    C -->|音频| F[音频编码]
    D --> G[Token IDs]
    E --> H[图像嵌入]
    F --> I[音频嵌入]
    G --> J[构建批次]
    H --> J
    I --> J
    J --> K[位置编码]
    K --> L[LoRA 适配]
    L --> M[嵌入层]
    M --> N[Transformer 层]
    N --> O{多层处理}
    O -->|Layer 1| P[自注意力]
    O -->|Layer 2| P
    O -->|Layer N| P
    P --> Q[前馈网络]
    Q --> R[残差连接]
    R --> S[层归一化]
    S --> T{是否最后层?}
    T -->|否| P
    T -->|是| U[输出投影]
    U --> V[Logits 计算]
    V --> W[采样器]
    W --> X[采样策略]
    X --> Y{采样类型}
    Y -->|贪婪| Z[选择最大概率]
    Y -->|Top-k| AA[保留top-k候选]
    Y -->|Top-p| AB[累积概率阈值]
    Y -->|温度| AC[温度缩放]
    Z --> AD[输出 token]
    AA --> AD
    AB --> AD
    AC --> AD
    AD --> AE[解码文本]
    AE --> AF{是否完成?}
    AF -->|否| B
    AF -->|是| AG[返回完整响应]

    subgraph KV 缓存数据流
        AH[Key 向量] --> AI[KV 缓存写入]
        AJ[Value 向量] --> AI
        AI --> AK[KV 缓存读取]
        AK --> P
    end

    P --> AH
    P --> AJ

    subgraph 内存管理
        AL[内存分配] --> AM[缓冲区]
        AM --> AN[内存释放]
        M --> AL
        V --> AN
    end
```

## 数据流说明

### 1. 输入处理流程
- **预处理**: 根据输入类型选择处理方式
- **分词/编码**: 将输入转换为模型可理解的格式
- **批次构建**: 合并多种输入到统一批次

### 2. 推理流程
- **位置编码**: 添加位置信息
- **LoRA 适配**: 应用 LoRA 权重调整
- **嵌入层**: 将 token 转换为向量表示
- **Transformer 层**: 多层注意力机制处理

### 3. KV 缓存流程
- **Key/Value 存储**: 保存每层的 Key 和 Value
- **缓存读取**: 复用之前计算的 KV
- **优化计算**: 减少重复计算

### 4. 采样流程
- **Logits 计算**: 输出层计算 token 概率
- **采样策略**: 选择采样方法
- **采样类型**: 贪婪、Top-k、Top-p、温度等

### 5. 输出生成
- **Token 选择**: 根据采样结果选择 token
- **解码**: 将 token 转换为文本
- **循环**: 继续生成直到完成

### 6. 内存管理
- **内存分配**: 为计算分配内存
- **缓冲区管理**: 管理计算缓冲区
- **内存释放**: 释放不再使用的内存